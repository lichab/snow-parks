import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        alignItems: 'center'
    },
    warning: {
        color: 'orange'
    },
    error: {
        color: 'red'
    }
})

export default styles